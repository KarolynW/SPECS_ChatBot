#TEL-ChatGPT

TEL-ChatGPT is a conversational AI system designed to assist users at the University of Hertfordshire with Technology Enhanced Learning (TEL) and general Teaching and Learning concepts. TEL-ChatGPT is built using OpenAI's GPT-3 language model and Flask web framework.

##Prerequisites

Before running TEL-ChatGPT, you need to set up the following:

*OpenAI API key
*Python 3
*Flask web framework
*Pandas, NumPy, and dotenv libraries

##Installation

1. Clone the repository to your local machine:

```
git clone https://github.com/YOUR-USERNAME/TEL-ChatGPT.git
```

2. Navigate to the project directory:

```
cd TEL-ChatGPT
```

3. Install the required libraries:

```
pip install -r requirements.txt
Set up the environment variables:
```

4. Create a file named .env in the project directory

5. Add your OpenAI API key to the .env file:

```
OPENAI_API_KEY=YOUR_API_KEY_HERE
```

##Usage

Run the Flask app:

python app.py
Access the chatbot via a web browser at http://localhost:5000.

##Contributing
Contributions are welcome! If you find any bugs or have suggestions for improvement, please open an issue or submit a pull request.

##License
This project is licensed under the MIT License - see the LICENSE file for details.
